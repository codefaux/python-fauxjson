[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fauxjson"
version = "0.2.5"
dependencies = []
[tool.pytest.ini_options]
addopts = "-s"

[project.optional-dependencies]
test = [
    "pytest>=9.0",
]